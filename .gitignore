# Build intermediates -- everything under target/ is regenerated by
# scripts/build.sh, except the final deliverable jar which we keep.
target/*
!target/Anwendung.jar
tools/*/target/
tools/*/dependency-reduced-pom.xml

*.class
.DS_Store
